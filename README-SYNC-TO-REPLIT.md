# Quotation-Wizard Ready — Full Design System Applied

This folder contains the complete, polished frontend from the authoritative reference (the local snapshot you provided from \\?\D:\Users\reza\Desktop\Quotation-Wizard).

It includes:
- The exact dark "operations control room" design system (index.css + tokens)
- The precise sidebar + layout used across the whole app
- All tuned UI components (especially button, card, badge, table, dialogs with Replit-specific styling)
- The full reusable table engine (use-table-controls + table-controls)
- Polished pages: Dashboard (Operations/Command Center), Enquiries (Client Pipeline), Calculator (fast Quotation Wizard), and all other modules

## How to apply to your live Replit project (https://replit.com/@reza93/Quotation-Wizard)

Fastest way in Replit editor:

1. Open your Replit project.
2. In the file tree, delete or replace the entire src folder with the one from this ready folder.
3. Also replace these root files if they differ:
   - vite.config.ts
   - package.json (merge dependencies if needed)
   - tsconfig.json
   - index.html
4. Drag and drop the src folder from this ready folder directly into the Replit file tree (Replit supports bulk upload).
5. For public assets, replace the public folder with the one here (logo.png etc.).
6. Refresh / restart the Repl.

After this sync, the entire app (all pages) will use the consistent Quotation Wizard design language you wanted for "the whole thing".

The local reference was the source of truth. This ready folder is a clean copy of it.

If you need the backend/api-server part updated too, let me know and I'll prepare that as well.
