---
description: Build the Enquiry → Quote automation pipeline per PIPELINE-SPEC.md
---

Read `PIPELINE-SPEC.md` in the repo root and implement it.

Work through the build phases (section 4) IN ORDER. After each phase: run `npm run build`, fix any errors, commit on branch `feat/quote-pipeline`, and push.

Obey the hard rules (section 0) — especially: NEVER send a client email/quotation without an explicit human click. No auto-send. Do not touch the Finance Center's finance tables.

Use the `frontend-design` skill for new UI and `systematic-debugging` if anything breaks. After each phase, summarise what you built and what's next.

Start with Phase 1 (schema migrations) and confirm the migration applied before moving on.

$ARGUMENTS
